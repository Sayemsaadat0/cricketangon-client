/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["i.pinimg.com","i.ibb.co.com", "placehold.co", "images.pexels.com", "localhost"],
  },

};

export default nextConfig;
