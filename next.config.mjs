/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["i.pinimg.com", "placehold.co", "images.pexels.com", "localhost", "p25.diginieit.com"],
  },

};

export default nextConfig;
